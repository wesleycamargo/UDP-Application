resources:
  repositories:
  - repository: UDP
    type: github
    endpoint: wesleycamargo
    name: wesleycamargo/UDP

trigger:
  - master
  
pool:
  vmImage: 'ubuntu-latest'

steps:

- template: src\orchestrator.yml@UDP
