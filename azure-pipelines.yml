resources:
  repositories:
  - repository: UDP
    type: github
    endpoint: wesleycamargo
    name: wesleycamargo/UDP

trigger:
  - master
  
pool:
  vmImage: 'ubuntu-latest'

extends:
  template: src\orchestrator.yml@UDP
  parameters:
    resourceType: dotnetcore

